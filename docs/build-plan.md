# THE CAP ROOM, Implementation Plan

**Goal:** Ship a deployable front-office console (cap sheet, trade validator, player eval) for NBA roster construction under the 2023 CBA, as a hiring demo for the Sacramento Kings Basketball Software Engineer role.

**Architecture:** Pure typed rules engine (`/engine`, zero UI imports) consumed by thin Next.js API routes, consumed in turn by three client modules. All data arrives as Zod-validated JSON snapshots in `/data`; no runtime external calls. State lives in React + URL params.

**Tech Stack:** Next.js 14 (App Router), TypeScript strict, Tailwind 3.4, Zod, Vitest, Python (`nba_api`) for offline ETL.

**Authoritative spec:** The user-authored CLAUDE.md spec in the kickoff message. Where this plan says "per spec §N", the spec's code fragments (constants §4, matching formula §5-R1, roster schema §3, golden tests §7) are the source of truth, they are not duplicated here to avoid divergence.

---

### Task 1: Scaffold
**Files:** Create `package.json`, `tsconfig.json` (strict), `next.config.mjs`, `tailwind.config.ts`, `postcss.config.mjs`, `app/layout.tsx`, `app/globals.css`, `vitest.config.ts`, `.gitignore`, `next-env.d.ts`.
- [ ] Pin next@14.2.x, react@18.3.x, tailwindcss@3.4.x, zod@3, vitest@2. `npm install` clean.
- [ ] `npm run build` passes on the empty shell. Commit `chore: scaffold`.

### Task 2: Engine, constants & types
**Files:** Create `engine/constants.ts` (verbatim spec §4), `engine/types.ts` (Player, TeamPicks, TeamCapSheet, TradeProposal, Verdict, RuleCheck per spec §3/§5).
- [ ] Constants match spec §4 to the dollar. Types compile under strict. Commit.

### Task 3: Engine, capsheet math (TDD)
**Files:** Create `engine/capsheet.ts`, `engine/__tests__/capsheet.test.ts`.
- [ ] Failing tests first: totals sum standard+dead (two-ways excluded), apron classification at exact boundaries, 209,015,000 → not above apron 1; 209,015,001 → above (spec §7.11); same strict-greater logic at tax and apron 2; floor flag below 148,465,000.
- [ ] Implement `computeCapSheet(teamCode, players)` → totals, status, distances-to-lines, available exceptions + hard-cap trigger copy. Tests green. Commit.

### Task 4: Engine, picks / Stepien (TDD)
**Files:** Create `engine/picks.ts`, `engine/__tests__/picks.test.ts`.
- [ ] Failing tests: trade leaving no owned first in two consecutive future drafts → fail; swap-encumbered pick still counts as present (spec §5-R7).
- [ ] Implement `checkStepien(picks, outgoingPickIds, incomingPicks)`. Green. Commit.

### Task 5: Engine, trade rules (TDD, the core)
**Files:** Create `engine/tradeRules.ts`, `engine/__tests__/tradeRules.test.ts`.
- [ ] Failing tests = golden scenarios spec §7.1–§7.10, including R1 formula sanity ($10M→$19,096,000; $3M→$6,250,000; $40M→$50,250,000).
- [ ] Implement `validateTrade(proposal, capSheets)` → `Verdict` with full `RuleCheck[]` ledger (R1–R8, hard-cap triggers as warnings per §5-R6; every check has plain-English `detail` with numbers).
- [ ] Aggregation v1 definition (document in code + README): a team aggregates when it sends 2+ matchable salaries and incoming exceeds the allowance generated by its largest single outgoing salary.
- [ ] 25–40 assertions green. Commit.

### Task 6: Data layer, schemas, loaders, template
**Files:** Create `lib/data/schemas.ts` (Zod: roster, picks, stats), `lib/data/load.ts`, `data/rosters/_TEMPLATE.json`, `scripts/validate-data.ts` (runs in CI + prebuild; malformed seed fails build).
- [ ] Loader test: template validates; a malformed fixture rejects with a readable error. Commit.

### Task 7: Seeds, SAC + 4–6 partners, picks
**Files:** Create `data/rosters/*.json`, `data/picks/SAC.json`, `data/meta.json` (seed date), `docs/sources.md`.
- [ ] Fetch current contract tables (Basketball-Reference /contracts/{TEAM}.html) for SAC + partners chosen to cover: one second-apron team, one cap-room team, one taxpayer. Record URL + access date per figure source in `docs/sources.md`.
- [ ] If fetch is unavailable, seed from best-known public figures and mark every affected file UNVERIFIED in `docs/sources.md` + README dev note (spec's builder-note fallback: Adelson re-verifies on build day). Never invent unknown values, omit + "unknown".
- [ ] Commit.

### Task 8: API routes
**Files:** Create `app/api/teams/route.ts`, `app/api/teams/[code]/capsheet/route.ts`, `app/api/players/route.ts`, `app/api/trade/validate/route.ts`.
- [ ] POST /api/trade/validate Zod-validates body, returns `Verdict`; curl examples verified by hand and pasted into README. Commit.

### Task 9: Shell
**Files:** Create `app/(shell)` layout pieces: `components/shell/Rail.tsx`, `TopBar.tsx`, `Footer.tsx`; fonts (Barlow Condensed display / Inter body, tabular numerals for money); palette per spec §8; `app/page.tsx` reviewer landing with module cards + 3 scenario shortcuts.
- [ ] Footer disclaimer exact per spec §0.6 with seed date from `data/meta.json`. Commit.

### Task 10: Trade Machine (flagship)
**Files:** Create `app/trade/page.tsx`, `components/trade/*` (TeamPanel, RosterPicker, CashControl, PickChips, RuleLedger, VerdictStamp, PostTradeThermos, ScenarioMenu), `lib/tradeUrl.ts` (proposal ⇄ query string).
- [ ] Ledger renders every RuleCheck (fails pinned top), monospaced rule ids, stamped LEGAL/ILLEGAL verdict; hard-cap warnings use spec copy pattern. Shareable URL round-trips. 3 preloaded scenarios (legal / apron-illegal / Stepien-illegal). Keyboard navigable, visible focus. Commit.

### Task 11: Cap Sheet
**Files:** Create `app/cap/page.tsx`, `components/cap/Thermometer.tsx` (hero, animated once, reduced-motion respected), `ContractTable.tsx` (sortable; dead money + two-ways sectioned), `ExceptionsPanel.tsx`, `MultiYearStrip.tsx`.
- [ ] Dollar distances to floor/cap/tax/apron1/apron2 all visible. Projections labeled as projections. Commit.

### Task 12: Player Eval + ETL
**Files:** Create `etl/pull_stats.py`, `etl/requirements.txt`, `data/stats/players-2025-26.json` (real run if network allows; else absent + UI shows "unknown"), `lib/percentiles.ts`, `app/players/page.tsx`, `components/players/*` (PlayerCard w/ initials monogram, CompareTray, RadarChart, PercentileBars).
- [ ] ETL: league-wide pulls, sleep between calls, incremental writes, `pulledAt` stamp. "Build trade around X" deep-link into /trade. Commit.

### Task 13: Docs + CI
**Files:** Create `README.md` (architecture mermaid, API curl docs, stats.nba.com cloud-IP note, known simplifications §5, GIF placeholder), `docs/production-notes.md` (Postgres schema sketch + ingestion DAG), `.github/workflows/ci.yml` (typecheck + test + data validation).
- [ ] Commit.

### Task 14: QA / ship gate
- [ ] `npm test`, `npm run build` green; drive all three scenarios in the browser; 390px pass; keyboard-only pass; reduced-motion pass; Cmd-K palette (stretch) if time.
- [ ] Run ship-audit skill (user's global rule) before calling it release-ready.
- [ ] Remaining user-only steps reported: verify/replace seed figures, Vercel deploy, README GIF.

**Self-review:** Spec coverage checked against §0–§10, every numbered requirement maps to a task above (Player Eval is Task 12 and is the designated cut if time-boxed, per §10). No placeholders beyond deliberate references to the authoritative spec. Type names used consistently: `Verdict`, `RuleCheck`, `TradeProposal`, `computeCapSheet`, `validateTrade`, `checkStepien`.
